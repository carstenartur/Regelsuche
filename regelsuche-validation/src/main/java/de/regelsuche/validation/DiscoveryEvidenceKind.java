package de.regelsuche.validation;

/** Evidence flags that describe what a discovery trace or report row demonstrated. */
public enum DiscoveryEvidenceKind {
    FACTORED,
    SIMPLIFIED,
    MACRO_LEARNED,
    MACRO_REUSED,
    EQUIVALENCE_VALIDATED
}
