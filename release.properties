# Regelsuche release metadata.
#
# The release workflow derives the release version from this SNAPSHOT value.
# The normal Actions dialog only selects patch, minor or major for the next
# development line; an exact non-standard next version may be reviewed in a
# [next=X.Y.Z-SNAPSHOT] release commit message.
version=0.2.0-SNAPSHOT
