# Regelsuche release metadata.
#
# The release workflow expects workflow_dispatch input release_version to match
# this value without the -SNAPSHOT suffix. The workflow updates this file
# together with CITATION.cff, .zenodo.json and codemeta.json.
version=0.1.4-SNAPSHOT
