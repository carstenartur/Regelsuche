/*
 * Embedded target-free held-out reproduction lifecycle.
 *
 * The base script registers host and verifier JavaExec authorities directly on
 * :regelsuche-discovery, where their main/test runtime classpaths are owned.
 * Root retains only orchestration and the stable compatibility aggregate.
 */

apply from: rootProject.file(
    'gradle/target-free-held-out-container-reproduction-base.gradle')
