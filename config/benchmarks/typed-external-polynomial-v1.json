{
  "schema": "regelsuche.typed-external-polynomial/v1",
  "claim": "PUBLIC_PREREGISTERED_PILOT_NOT_INDEPENDENT_BLIND_TEST",
  "information": "SOURCE_ONLY_EVALUATION_AFTER_MODEL_FREEZE",
  "objective": "MINIMUM_SURFACE_ARITHMETIC_OPERATIONS_WITH_EXACT_EQUIVALENCE",
  "sympyVersion": "1.14.0",
  "profiles": ["TYPED_PRIMITIVE", "TYPED_REORDERED", "TYPED_PRIMITIVE_SELECTED", "TYPED_LEARNED", "TYPED_LEARNED_SELECTED", "SYMPY_FACTOR", "SYMPY_PORTFOLIO"],
  "workBudgets": [128, 2048],
  "repetitions": 3,
  "requestSeconds": 2,
  "maxSearchDepth": 12,
  "maxPrimitiveDepth": 12,
  "maxStates": 256,
  "warmup": "a+1",
  "training": "TraceStrategyTransferExample.trainingInputs/inventory/limits at main 81ea9ac7; TypedLearningWorkStudy TRAIN policy selection only",
  "fixedOrder": ["add-zero", "multiply-one", "square-product", "cancel-addend", "double-term", "difference-product", "factor-left", "factor-right"],
  "costPolicy": "PER_PROFILE_SETUP_TRAIN_WARMUP_APPLICATION_AND_VERIFICATION;NO_FREE_BEST_OF;NO_LOGICAL_WORK_TO_CAS_CPU_CONVERSION",
  "failurePolicy": "RETAIN_EVERY_ROW;NO_OVER_BUDGET_SUCCESS;NO_POST_EVALUATION_TUNING",
  "cases": [
    {"id":"compound-product", "source":"((a*b+c)*(a*b-c)+c*c)*(a+11)", "supported":true},
    {"id":"compound-sum", "source":"((a+b+c)*(a+b-c)+c*c)+(d*d+d*a)", "supported":true},
    {"id":"two-sites-product", "source":"((a+b)*(a-b)+b*b)*((c+d)*(c-d)+d*d)", "supported":true},
    {"id":"two-sites-sum", "source":"((a+b)*(a-b)+b*b)+((c+d)*(c-d)+d*d)+13", "supported":true},
    {"id":"nested-sites", "source":"((a+b)*(a-b)+b*b)*(((c+d)*(c-d)+d*d)*(a+17))", "supported":true},
    {"id":"rational-context", "source":"((a+b)*(a-b)+b*b)/7+(c*c+c*d)/11", "supported":true},
    {"id":"rectangle-control", "source":"a*c+a*d+b*c+b*d", "supported":true},
    {"id":"nested-common-factor", "source":"(a*b+a*c)*(d+17)+(a*b+a*c)*(d+19)", "supported":true},
    {"id":"right-factor-context", "source":"(a+c)*b+(a+c)*d+0", "supported":true},
    {"id":"coefficient-control", "source":"9*a+13*a+2", "supported":true},
    {"id":"degree-six-control", "source":"a^6-b^6", "supported":true},
    {"id":"zero-power-control", "source":"(a+b)^0+c*c", "supported":true},
    {"id":"noncancelling-residual", "source":"((a+b)*(a-b)+b*(b+c))*(d+3)", "supported":true},
    {"id":"simple-zero", "source":"a*b+c+0", "supported":true},
    {"id":"already-simple", "source":"a+b+c+d+23", "supported":true},
    {"id":"quartic-control", "source":"a^4+10*a^2+9", "supported":true},
    {"id":"unsupported-variable-denominator", "source":"a/b", "supported":false},
    {"id":"unsupported-logarithm", "source":"log(a)", "supported":false},
    {"id":"unsupported-negative-power", "source":"a^-1", "supported":false}
  ]
}
