# Discovery Benchmarks

The `regelsuche-benchmarks` module defines a deterministic discovery benchmark suite.

Initial categories cover factorization, trigonometry, rational telescoping, and complete-square examples such as `x^2-y^2`, `sin(2x)`, `1/(n(n+1))`, and `x^2+6x+5`.
