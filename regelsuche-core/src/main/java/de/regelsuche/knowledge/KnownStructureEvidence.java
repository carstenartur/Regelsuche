package de.regelsuche.knowledge;

/** Minimum evidence required before a known form unlocks capabilities. */
public enum KnownStructureEvidence {
    OBSERVED, VALIDATED_BY_EXAMPLES, SYMBOLICALLY_VERIFIED,
    FORMALLY_PROVABLE, FORMALLY_PROVED
}
