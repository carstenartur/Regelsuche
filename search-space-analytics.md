# Search Space Analytics

`SearchSpaceAnalyticsService` summarizes proof-search transitions with states explored, unique canonical states, convergent states, learned macro usage, and average branching factor.

`SearchSpaceGraphExporter` emits DOT graphs that can highlight discovery paths, macro paths, dead ends, and convergence nodes. `MacroImpactReport` compares analytics before and after macro use.
