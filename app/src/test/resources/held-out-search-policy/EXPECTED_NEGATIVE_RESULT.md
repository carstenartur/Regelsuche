The current empirical model is keyed by concrete primitive rule ID. The held-out
power-normalization and quartic-factorization tasks use primitive rule IDs absent
from the neutral-element training family. The expected safe behavior is therefore
per-candidate static fallback. A measured lack of improvement identifies the need
for rule-structural and transition-delta features; it must not be reported as a
learned cross-family success.
