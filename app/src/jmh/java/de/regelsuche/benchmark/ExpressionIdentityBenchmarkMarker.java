package de.regelsuche.benchmark;

/** Keeps the ADR #242 benchmark suite explicitly discoverable in pull-request diffs. */
final class ExpressionIdentityBenchmarkMarker {
    private ExpressionIdentityBenchmarkMarker() {
    }
}
