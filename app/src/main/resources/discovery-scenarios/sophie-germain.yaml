id: sophie-germain
displayName: Sophie-Germain hidden structure
inputExpression: "x ^ 4 + 4 * y ^ 4"
targetExpression: "(x ^ 2 - 2 * x * y + 2 * y ^ 2) * (x ^ 2 + 2 * x * y + 2 * y ^ 2)"
expectations:
  - BRIDGE_REQUIRED
  - MACRO_LEARNING_REQUIRED
  - MACRO_REUSE_REQUIRED
enabledRulePacks:
  - sophie-germain
requiredBridgeEffects:
  - BRIDGING
requiredRuleFamilies:
  - hidden_structure
  - factor
requiredBridgeRules:
  - bridge_sophie_germain_hidden_square
  - ast_square_difference_factor
macroLearning:
  enabled: true
  expectedMacroRule: macro_learned_sophie_germain_factorization
budgets:
  maxDepth: 6
  maxStates: 120
  timeoutMillis: 5000
gallery:
  generateSvg: true
  preferredPathCount: 2
  minVisibleNodes: 8
