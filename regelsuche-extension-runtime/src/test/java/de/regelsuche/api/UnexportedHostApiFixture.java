package de.regelsuche.api;

/** A public host type sharing the lifecycle-annotation package, but not its export grant. */
public final class UnexportedHostApiFixture {
    private UnexportedHostApiFixture() { }
}
